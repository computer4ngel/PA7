#include "PA7.h"
#include "Calculator.h"
#include <fstream>


// TODO: your helper functions here

void mainOperation() {
Calculator test;
    test.convertInfixToPostfix("A+B");

    /*string value, expression;

    ifstream inFile;
    inFile.open("input.txt");
    Calculator input;

    while(inFile >> value) {
        if(value == "#")
            break;
        else {
            cout << value << endl;
        }
    }
    while(inFile >> expression) {

        if(expression == "#")
            break;
        else {
            cout << expression << endl;
            input.convertInfixToPostfix(expression);
        }
    }*/
}
